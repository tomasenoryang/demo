#include <iostream>
#include "utils.h"

int main() {
    int a = 3, b = 4;
    std::cout << "Add: " << add(a, b) << std::endl;
    std::cout << "Multiply: " << multiply(a, b) << std::endl;
    return 0;
}
